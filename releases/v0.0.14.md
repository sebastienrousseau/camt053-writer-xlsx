# camt053-writer-xlsx v0.0.14

Suite-wide lockstep release aligned to the camt053 suite v0.0.14 line.
No functional changes to the writer.

Dependency refresh only: this release targets the `0.0.14` release of
`camt053` (the existing `camt053 >= 0.0.6, < 1` constraint already
admits it).
